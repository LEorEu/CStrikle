# Blind Draft:实现现状、决议与数据

**正式设计稿是 `FribergCS2_Blind_Draft_System_Design_v0.2.md`。** 那份是玩法蓝图,
这份是它的落地记录:哪些已经实现、实现时改了什么、为什么改、以及用真实数据核对
出来的数字。玩法讨论的原始素材在 `ChatGPT-小项目反馈与建议.md`。

**可玩原型与真人试玩的结论在 `DESIGN_DRAFT_PROTOTYPE.md`。** 那份记录推翻了这份
文档里的几条推测(尤其是 §7.4 的线索方向和「贵卡透明不是 bug」),下面第四节的
待办排序应以那份为准。

生成器 `scripts/gen_draft_cards.py`(当前 `CARD_VERSION = v6`),产出
`data/draft_cards.json`(生成物,648 张)。人工层两个文件:
`data/manual/player_overrides.json`(位置真值)和
`data/manual/draft_overrides.json`(数值平衡 + 排除名单)。

---

## 核心判断(整轮讨论的出发点)

**老观众和新手要的东西相反。** 猜选手模式里六个线索维度给完,老观众等于直接
看到答案(只是回忆检索,没有推理);新手给完也还是不知道是谁,因为脑子里没有
650 人的库。用户本人自评接近新手水平,直接玩对战吃力 —— 这是设计的第一约束。

Blind Draft 之所以成立,正是因为它**藏掉身份**:玩的是从残缺信息里推断价值,
而不是回忆人名。新手第一局就能玩,老手也不会腻。

---

# 一、已实现:选手卡生成体系

## 1.1 全库分布(v6)

```
卡池 648 张 | 待定位置 0 人 | 人工确认排除 3 人

       RIFLER    AWPER      IGL      合计
G5         22        7        5      34
G4         23        5        7      35
G3         56       18       13      87
G2        102       25       56     183
G1        187       52       70     309

         火力   领导   经验   稳定    overall 最低~中位~最高
G5        91     30     85     84     83.7 ~ 85.4 ~ 87.8
G4        81     27     70     75     73.0 ~ 75.6 ~ 78.3
G3        70     24     54     65     60.4 ~ 64.2 ~ 66.5
G2        59     23     40     56     50.2 ~ 53.4 ~ 56.3
G1        51     20     23     49     41.0 ~ 43.6 ~ 46.9

位置中位  步枪 51.2 | 狙 51.7 | 指挥 51.9      三者持平,没有位置统治
经济锚点  G5 中位 85.4 / G1 中位 43.6 = 1.96 倍(价格差 5 倍)
```

## 1.2 算法规格(自动生成,勿手改)

下面这一块由 `python scripts/gen_draft_cards.py --spec` 生成,`tests/test_draft_cards.py` 会断言它和代码里的常量一字不差。
改了公式或常量,重新跑一次贴回来即可;各条规则**为什么**是这样,见后面的小节。

<!-- SPEC:BEGIN 由 scripts/gen_draft_cards.py --spec 生成,勿手改 -->

**CARD_VERSION = `v6`**

### 判定顺序

```
1. 位置    played_role(人工层) > primary_role(主游戏) > roles 裸标签
           三者都拿不到 → 不进卡池(待定);draft_exclude 命中 → 确认排除
2. 档位    通用通道 与 指挥通道 取更高的一档
3. 模板    TEMPLATE[位置][档位] 给出四维底板
4. 修正    corrections() 的增量(见下)
5. 抖动    档位 ∈ EVIDENCE_GRADES 时为 0;否则火力 ±4、其余 ±3
           种子 = blake2b(page + CARD_VERSION),同一版本下永远同一张卡
6. 取整    round() 后夹在 [1, 99]
7. 覆盖    draft_overrides.json;其中 grade/position 在第 3 步之前生效,
           四维为直接替换。overall 最后按覆盖后的值重算
```

### 通用档位(自上而下互斥,取第一个满足的)

```
G5  HLTV Top20 进过前五
G4  Top20 上榜 >= 2 次
G3  上榜 1 次 或 有 Major 冠军 或 打进过 Major 决赛(最好名次 <= 2)
G3  或 年龄 <= 24 且 当前有队 且 Major >= 3 且 最好名次 <= 8
G2  Major >= 3 次
G1  其余

名次取自 majors[].placement。上游只记录前八名,空值即「9 名开外」,
所以「推不出冠军」是正确结论而不是数据缺失。
```

### 指挥通道(只上调,不下调)

```
igl_score = Σ(冠军 1.0 + 决赛 0.4 + 四强 0.2) × 年代权重
            + 出场次数 × 0.02
门槛      >= 4.2 → G5   >= 2.2 → G4   >= 0.8 → G3
```

### 年代权重(Top20 与指挥通道共用)

```
weight(年份) = max(0.78, 1 - 0.018 × (参照年 - 年份))
参照年 = hltv_top20.json 快照里最新的年份,不用系统当前年——否则每跨一个
自然年全库数值会集体漂移一次。
```

### 四维修正 corrections()

```
火力   有 Top20 时: min(top20_score / FIRE_ANCHOR[档], 1) × 6.0
       其中 top20_score = 最好一年的名次分 + 其余年份 × 0.2
       名次分 = 0.78 ^ (名次 - 1),再乘年代权重
       位置为 IGL 时只继承 0.4 倍(避免明星履历 + IGL 模板叠成六边形怪物)
经验   Major 次数 → 9 × log1p(次数) / log(21)   边际递减
       每个冠军 +1.5(最多计 4 个)
领导   位置为 IGL 时: min(igl_score / LEAD_ANCHOR[档], 1) × 6.0
火力   年龄 <= 22 且档位不在 EVIDENCE_GRADES: +2.5
稳定   年龄 <= 22: -5(不分档位,打得飘是事实,和证据多少无关)
```

### 常量

| 常量 | 值 | 作用 |
|---|---|---|
| `FIRE_SPREAD` | 6.0 | Top20 履历在同档内能拉开的火力幅度 |
| `LEAD_SPREAD` | 6.0 | 团队荣誉在同档内能拉开的领导力幅度 |
| `TOP20_DECAY` | 0.78 | 名次分几何衰减,#1=1.00 #5=0.37 #10=0.11 |
| `TOP20_DEPTH` | 0.2 | 非最佳年份的折算权重 |
| `TOP20_RECENCY` | 0.018 | 每早一年的衰减 |
| `TOP20_RECENCY_FLOOR` | 0.78 | 年代权重地板 |
| `FIRE_ANCHOR` | {5: 2.0, 4: 0.36, 3: 0.3, 2: 0.3, 1: 0.3} | 拿满 FIRE_SPREAD 所需的年度积分 |
| `LEAD_ANCHOR` | {5: 6.5, 4: 3.5, 3: 1.6, 2: 1.0, 1: 1.0} | 拿满 LEAD_SPREAD 所需的指挥分 |
| `EVIDENCE_GRADES` | (5, 4) | 这些档位不掷骰子 |

锚点是**固定常量**而不是「池内最高分」:按池内最高归一化的话,将来入库一个
更强的人会让全库旧卡悄悄变数值。改这些值要手动 bump `CARD_VERSION`。

### Grade × Position 模板 (火力/领导/经验/稳定)

| 档 | RIFLER | AWPER | IGL |
|---|---|---|---|
| G5 | 89/30/75/84 | 89/26/75/88 | 66/90/76/77 |
| G4 | 80/27/62/75 | 80/24/62/80 | 60/84/65/71 |
| G3 | 70/24/48/65 | 70/22/48/70 | 54/75/52/64 |
| G2 | 60/22/34/56 | 60/20/34/60 | 49/65/38/57 |
| G1 | 52/20/20/48 | 52/18/20/51 | 45/56/25/50 |

### 位置权重(只用于内部估值 / STEAL 判定,不驱动比赛模拟)

| 位置 | 火力 | 领导 | 经验 | 稳定 |
|---|---|---|---|---|
| RIFLER | 0.55 | 0.05 | 0.2 | 0.2 |
| AWPER | 0.45 | 0.05 | 0.2 | 0.3 |
| IGL | 0.25 | 0.35 | 0.35 | 0.05 |

<!-- SPEC:END -->

---

## 1.3 分档规则(§3.2 的实现版,与原稿有出入)

自上而下互斥,一个人只落一档:

| 档 | 条件 |
|---|---|
| G5 | HLTV Top20 进过前五 |
| G4 | Top20 上榜 ≥2 次 |
| G3 | 上榜 1 次 / Major 冠军 / **打进过 Major 决赛** / **≤24 岁 + 有队 + Major≥3 且进过前八** |
| G2 | Major ≥3 次 |
| G1 | 其余 |

**与 v0.2 §3.2 的三处差异,都是实测发现的悬崖:**

1. **G3-B 从「冠军」放宽到「决赛」。** 原规则下同样是决赛打过一场,赢了升两档、
   输了一分不给,一场 BO3 值 18 点底板火力。全库有 39 人最好成绩是亚军,其中
   10 人落在 G1(jcobbb 22 岁、FaZe、2025 布达佩斯亚军,只打过这一届)。
2. **G3-C 年轻潜力股必须另有 Major 深度。** 原规则「≤24 岁 + 有队 + Major≥3」
   占满了 G3 的 44%(48/110),其中 36 人一次都没进过 Major 前八——对名额宽松的
   赛区几乎是白送。典型症状:TYLOO 的 Jee(21 岁,4 届全部止步 9 名开外)进 G3、
   底板火力 70,而同队同战绩的 JamYoung 只因为 25 岁掉到 G1、底板 52,**一个生日
   值 18 点火力**。年轻人该得的是 §15 的档内特色(火力上限 +、稳定 −),不是跨两档。
3. **G2 只看出勤,不看是否现役。** 在编与否是当前状态,不该改变生涯证据等级
   (§14 一律按生涯代表版本评价)。带这个条件会让打过 4 届 Major 还拿过亚军的
   退役选手(JACKZ/nexa/steel/isak)掉到 G1。

## 1.4 指挥单开一套证据(v0.2 没有的东西)

**HLTV Top20 是个人火力奖,结构性地排除指挥**——全库 158 个 IGL 只有 9 个上过榜,
却有 20 个拿过 Major 冠军。拿 Top20 给指挥分档的结果是 gla1ve(4 冠)、
karrigan(2 冠 6 决赛)、pronax(3 冠)全落 G3,而带着一个 `igl` 标签、靠个人数据
上榜的枪手占满 G4/G5。

指挥改用团队荣誉计分,**只上调不下调**(靠 Top20 上去的不受影响):

```
igl_score = Σ(冠军 1.0 + 决赛 0.4 + 四强 0.2)×年代权重 + 出场 ×0.02
分档门槛   ≥4.2 → G5     ≥2.2 → G4     ≥0.8 → G3
档内排序   领导力 = 模板 + min(score/锚点, 1) × 6.0     锚点 {G5:6.5, G4:3.5, G3:1.6}
```

结果:

```
apEX 7.34 分 4冠 → G5 领96      karrigan 6.19 分 2冠 → G5 领96
gla1ve 6.07 分 4冠 → G5 领96     pronax 4.52 分 3冠 → G5 领94
G5 IGL(5) apEX 85.8 · gla1ve 85.4 · karrigan 84.7 · FalleN 83.9 · pronax 83.7
G4 IGL(7) Xizt · Zeus · TaZ · Jame · Boombl4 · Aleksib · blameF
```

## 1.5 火力:年度积分取代原公式

原公式 `(6−最好名次)×1.4 + (上榜次数−1)×1.1` 有三个毛病:名次只在前 6 有区分度
(第 7 和第 20 完全等价)、次数权重几乎等于名次权重(ZywOo 的 4 个年度第一算出来
反而低于 s1mple 的 3 个第一 + 5 次中游)、以及**抖动比履历分还大**——上一版
sh1ro 靠 +3.53 的随机拿到火力 99,ZywOo 吃 −3.80 掉到 93,卡上写的不是谁更强,
是谁掷得好。

现在:

```
名次分   0.78^(rank−1)         #1=1.00 #2=.78 #3=.61 #5=.37 #10=.11 #20=.01
年度积分 最好的一年全额 + 其余年份 ×0.20
年代权重 每早一年 −1.8%,地板 0.78(最老的 2013 正好触底)
火力     模板 + min(积分/锚点, 1) × 6.0      锚点 {G5:2.00, G4:0.36, G3:0.30}
G5/G4 不掷骰子   有 Top20 就足够排序;G3 及以下证据不足,随机是「高方差」定位的一部分
```

参照年取快照里最新的年份(2025)而不是系统当前年,否则每跨一个自然年全库数值会
集体漂移一次,又是一次不会报错的静默变更。

火力前六:`donk 96 · ZywOo 95 · m0NESY 94 · s1mple 94 · dev1ce 93 · NiKo 93`,
没有任何人被 99 截断(上一版有 2 个)。

## 1.6 AWPER 火力底板与 RIFLER 持平

v0.2 §17.2 原表给狙 +3 火力(G5 92 vs 89)。后果是除 NiKo 外所有 G5 狙都压过所有
G5 步枪——一个由模板而非履历造成的系统性错位。现已拉平,狙的特色留在稳定那一列
(88 vs 84)。

## 1.7 位置权重:IGL 领导力 .45 → .35

指挥单开证据通道之后,IGL 的 overall 中位数一度升到 53.6,压过步枪 51.5 和
狙 52.0,三个位置里最高。降到 `.25/.35/.35/.05` 后回到 51.9,正好夹在两者之间。

**这只是定价上的折价,不是说指挥不重要。** 按 `ChatGPT-小项目反馈与建议.md`,
指挥的真正价值有一部分在队伍层(Leadership → Map Pool / BP / 战术类 Buff),
那部分现在还没实现。等模拟层做出来要回来重看这个权重,否则就是同一件事折价两次。

## 1.8 位置真值:三层 + 一个自动校验

**优先级 `played_role` > `primary_role` > `roles` 裸标签,顺序不能反。**
主游戏的 `game_role` 是「当前身份」(Magisk 现在是 BC.Game 的指挥、gla1ve 现在是
100T 的教练),而 Draft 按 §14 要的是「生涯代表版本」(Magisk = Astralis 时期的
步枪手)。人工层 `played_role` 的语义正好是选手期位置,所以它必须压过 game_role。
反过来读会让所有转型选手拿到错的模板。副作用为零:给主教练或已有 game_role 的人
加 `played_role`,主游戏显示完全不变。

**Liquipedia 的 `igl` 标签只要这人生涯里指挥过就打**,分不出「巅峰是指挥」和
「后来当过指挥」。人眼看名单必然会漏,所以加了一条自动校验:

> **一支冠军阵容不可能有两个指挥。**

24 支冠军阵容跑一遍,曾抓出 8 支异常,全部是把夺冠时的枪手误判成指挥:

```
2014 Katowice  virtus.pro     Snax、TaZ        → 真指挥 TaZ
2016 Columbus/Cologne  sk/luminosity  FalleN、TACO → 真指挥 FalleN
2017 Krakow    gambit         Dosia、Zeus      → 真指挥 Zeus
2018 Boston    cloud9         Stewie2K、RUSH   → 真指挥 Stewie2K
2021 Stockholm natus vincere  Boombl4、Perfecto → 真指挥 Boombl4
2022 Rio       outsiders      Jame、Qikert     → 真指挥 Jame
2024 Shanghai  spirit         chopper、magixx  → 真指挥 chopper
```

七人已改判为步枪手,检查现在通过。**这个校验永久留在体检里**,将来重建数据库
或上游改标签时会自动重新报警。

## 1.9 幂等

设计稿 §16.1 要求卡牌 RNG 只在生成时跑一次。实现方式不是保存随机状态,而是让
生成器**幂等**:种子取自 `hash(page + CARD_VERSION)`,同一个人在同一版本下永远
算出同一张卡。同理,所有归一化锚点都是**固定常量**而非「池内最高分」——按池内
最高归一化的话,将来入库一个更强的人会让全库旧卡悄悄变数值。

改数值要手动 bump `CARD_VERSION`,那是一次自觉的全库重算。

---

# 二、已定的决议

1. **v1 闭环** —— Buff 卡与后续应用方式已想好,不存在「省钱没有出口」的问题。
2. **教练** —— 走 `played_role` 回退;判定为纯教练的人用 `draft_exclude` 显式
   排除,和「还没人看过」分开计数。混在一张名单里的话,下次体检还得重新辨认哪些
   已经处理过,时间一长就没人看了。当前排除 3 人:zonic、pita(教练)、
   Jab Jabich(彩蛋,一只青蛙玩偶,2026 科隆随 Spirit 以教练身份出场;留在主游戏
   题库,不进卡池)。
3. **线索分配** —— 放弃 §7.2「每轮五种线索各用一次」的硬约束。那条和 §7.4
   「不要死线索」直接打架:每轮必有 $1/$2 卡,而这两档冠军数 100% 为 0。
   改成按档位加权:$5/$4 可用冠军数,$3 少用,$2/$1 禁用。
4. **保护名单** —— s1mple / donk / ZywOo 这类锁死 $5,不参与降档。
5. **IGL 限价** —— **待重定**。原决议是从 $3 提到 $4,理由是「同档下 IGL 与步枪
   强度几乎相等」。现在三个位置的价格阶梯已经完全平行(见三·1),限价反而会造出
   反向错价:把一张 84.7 的卡塞进 $4 的格子,而 $4 的正常货是 75.6,**白送 9.1,
   正好一整档**。建议取消限价,理由见三·2。
6. **价格差 5 倍 / 强度差 1.96 倍** —— 交给肉鸽方差处理。注意:**方差不改变期望**。
   这条在淘汰制结算下成立(弱队本就该赌方差),在排行榜式线性结算下不成立。
7. **升档** —— 允许。顺带解决卡池深度。但**不能靠放宽价格带来制造盲区**,见三·3。
8. **阵容是软惩罚** —— 可以不买 IGL/AWP,但 Reveal 阶段吃 debuff。因此 Board
   生成器不需要位置兜底。
9. **卡片数据库必须是生成物** —— 项目已经有两个权威源(爬虫生成物 / 人工层),
   再加一个手工维护的第三方数据库一定会烂掉。照抄现有模式:
   `players.json + hltv_top20.json + 人工层 → gen_draft_cards.py → data/draft_cards.json`。
10. **平衡用 Override,不改公式** —— §21 Algorithm First, Override Last。当前
    `draft_overrides.json` 只有两条数值修正:`donk 火力 96`(公式只看得到 2024/2025
    两个赛季,但他就是公认火力第一)、`m0NESY 火力 94`。每条都带 `reason`。

---

# 三、价格与盲性:实测数据

## 3.1 三个位置的价格阶梯完全平行

```
        RIFLER        AWPER         IGL       同价位差(以步枪为基准)
G5   85.4 (22人)   85.8 ( 7人)   84.7 ( 5人)   狙 +0.4 / 指 −0.7
G4   75.6 (23人)   76.0 ( 5人)   75.5 ( 7人)   狙 +0.4 / 指 −0.1
G3   63.7 (56人)   64.9 (18人)   64.7 (13人)   狙 +1.2 / 指 +1.0
G2   53.0(102人)   53.8 (25人)   53.5 (56人)   狙 +0.8 / 指 +0.5
G1   43.9(187人)   44.1 (52人)   42.9 (70人)   狙 +0.2 / 指 −1.0

每块钱换到的 overall:  步枪 9.1~10.6   狙 9.7~10.6   指挥 10.6~10.9
```

**位置税不存在。** 所以「堆枪男 + 便宜指挥」是完全可行且中性的流派:把指挥从 $5
降到 $1 亏 41.8,把一个枪手从 $5 降到 $1 亏 41.5,差别在噪声里。而且供给上永远
做得到——**G2 有 31% 是指挥**(56 个),$2 那格随时能捡到指挥。

这也意味着**位置本身在经济层不产生决策**:玩家看到 `$3 IGL` 和 `$3 RIFLER`,
从卡面价值上没有理由偏好谁,只有「我还缺哪个位置」这一个理由。这是对的——让位置
有意义的不该是数值,而应该是**阵容要求 + 稀缺性 + 围绕位置的 Buff**
(`DOUBLE AWP SETUP` 要两个能打狙的,`DEEP MAP POOL`/`TACTICAL PREPARATION`
走指挥的 Leadership)。指挥的价值应该由「他解锁了什么」体现,而不是「他 overall 高」。

## 3.2 贵卡是明牌:$5 指挥一给线索就露底

用主游戏 solver 那套「期望剩余候选数」量化每一格的盲性(`1.0` = 这条线索等于身份证):

```
                候选    国籍   俱乐部   年龄  Major次数  冠军数
$5 IGL            5     1.4    1.0     1.4     1.4      1.8   ← 给什么线索都露底
$5 AWPER          7     1.3    1.0     1.3     1.9      2.1
$4 AWPER          5     1.0    2.2     1.4     1.8      2.6
$4 IGL            7     1.3    1.0     1.6     1.9      5.3
$5 RIFLER        22     2.5    7.1     2.1     2.5      7.7
$1 RIFLER       187     9.5   61.9    10.3    99.0    187.0
```

结构是**倒挂的**:越贵越透明,越便宜越盲目。$5 指挥那五个人分别在
Vitality / 100T / Falcons / FURIA / 自由身,俱乐部线索一给就唯一确定。

而且这个透明**只对老手成立**——新手不知道「顶级指挥只有五个」,他看到
`$5 IGL · France` 照样一脸茫然。

## 3.3 放宽价格带这条路走不通

要把 $5 指挥的候选从 5 个撑到 25 个才算真盲,代价是:

```
$5 那一格的期望价值
  纯 G5      指挥 84.7 (5人)    步枪 85.3 (22人)
  G5+G4      指挥 79.2 (12人)   步枪 80.3 (45人)
  G5+G4+G3   指挥 71.5 (25人)   步枪 71.0 (101人)
```

**盲区造出来了,但价格信号死了**:花五分之一预算买个 G3 的风险太大,理性玩家会
直接放弃 $5 这一格。而且这不是指挥独有的问题,步枪一样掉到 71.0。CS 历史上够格的
顶级指挥就只有五个,这是数据集的物理事实,任何机制都变不出第六个。

## 3.4 换个角度:贵卡透明可能不是 bug

认出身份 ≠ 知道该不该买。老手仍然要判断「$5 买一个 84.7 的指挥,还是留着这 $5
去买两张 Rogue 卡 + 一个 $1 淘宝」,决策一点没少。

而这恰恰是「老观众和新手要的东西相反」的解法:在猜人模式里,老手的知识变成
**无聊**(一眼看到答案);在 Blind Draft 里,同样的知识变成**估值优势**(他读得懂
市场,知道这张 $4 的丹麦指挥是 gla1ve 因而是白捡)。新手那一侧完全没受影响,而且
赛后 Reveal 会告诉他这是谁——正是文档里说的「新手通过实际对局自然认识选手」。

**真正坏掉的是那张卡上的线索位。** 在 5 人格子里线索已经不提供任何信息了,
老手看一眼价格和位置就知道是谁,那条「俱乐部:Vitality」纯属浪费一个信息位。

所以 §7.4 要补上另一半:文档说「弱线索可以存在,死线索不应存在」,现在还要加
——**「唯一识别」线索也不该存在**。它不是太弱,是太强,强到把这张卡的盲性直接
抵消。线索匹配应该瞄准一个**区间**,而不是越强越好。

---

# 四、待办(按优先级)

> 2026-08-27 更新:真人试玩之后这个排序已经过时,以
> `DESIGN_DRAFT_PROTOTYPE.md` 第六、七节为准。四条要点:
>
> 1. 线索问题的根不在「给什么线索」,而在**价格直接播报了档位**——$5 就是 G5 的
>    别名,所以认出名人就等于知道答案。现在价格与档位分离(先抽到人,市场再为他
>    报价,报价会错),卡面给的是**球探区间**而不是精确数字。
> 2. 同价位的卡确实是克隆人(模板 + 抖动),但解法不是给同档编一套实力差
>    (试过,违反 §14「按生涯代表版本评价」),而是让**同一个标价下可能站着不同
>    档位的人**。
> 3. 板面供给(按钱发牌、按需给位置)比线索更早卡住玩家。
> 4. 十一局之后,**缺的已经不是决策质量**——最好的一局是 49/59577,贴着这批牌的
>    数学最优,作者仍然说「还差一点味道」。缺的是**选完之后发生什么**:四维至今
>    没有兑现场景。下一个里程碑是极简 Run(BO1 / BO3 / 高压淘汰局),不是继续调
>    线索或权重。

1. **线索系统**(§6/§7 的实现)。三件事一起做:按档位加权避开死线索(决议 3)、
   给小格子改发**价值线索**而不是身份线索(比如 $5 IGL 那张卡与其写「France」,
   不如写「领导力 96」——对新手能用来估值,对老手不泄露任何新信息)、以及
   线索强度设上限。
2. **Market Price / Board Generator**(§5/§6)。IGL 限价按新数值重定(决议 5);
   另一条可选路线是**板面价格组合本身随机化**(不是每轮固定 $5..$1 各一张),
   这样「这局出不出得起 $5」本身成为抽卡,顺带解决 §8.3 的预算死局。
3. **队伍层属性**。`ChatGPT-小项目反馈与建议.md` 的最终模型是六项:
   Firepower / Leadership / Experience / Chemistry / Stability / **Map Pool**。
   指挥的产出走这条管道:
   ```
   队伍 Leadership = IGL 的 leadership ×0.7 + 其余四人 ×0.3
   队伍 Map Pool   = f(队伍 Leadership, 队伍 Experience)   ← BP 阶段吃这个
   ```
4. **数值上限的两个世界**(讨论已定,未实现)。卡面固定 `1–99`,比赛内的
   Effective Performance **可以超过 99**,超出部分按 40~50% 递减生效(Soft Cap)。
   顶级选手不会因为顶到天花板而浪费 Buff,普通选手 Life Game 也真能打出历史级
   单场。注意:soft cap 系数必须和 Stability 的 roll 幅度一起定,否则普通选手
   永远碰不到 soft cap 区间,这个机制就只是在给顶级选手削 Buff 收益,方向反了。
5. **`data/major_results.json` 补 IEM Katowice 2019**(Astralis 夺冠)。
   `placement` 只有 36% 有值不是数据缺失——上游只记录前八名,空值等于「9 名开外」,
   推出「没拿过冠军」是正确结论。25 场 Major 里 24 场都有冠军记录,只缺这一场。
6. **32 → 0 已清零**:所有教练的选手期位置都已人工填完,`待定位置 0 人`。

---

# 五、已撤回

**线索采购模式**(开局什么都不给,花预算逐条买线索)。死结:猜错之后给不给属性
对比?给 → 猜一次白拿一整行,买卖失去意义;不给 → 新手在 650 人里盲猜,比现在
更绝望。矛盾在结构上,调价格调预算都救不了。

**早期的「档位 = 池子 + 价格 + 线索」三合一设计**。已被 v0.2 的
Grade / Price / Power 三分离取代 —— 后者明显更对。

---

# 六、暂存(未展开)

- **跨模式的统计 / 图鉴层**。用户明确要,且要求「不同模式里都可以有」——这句话
  定了架构:必须先做成一层**共享存档**,否则做到第三个模式就得推倒重来。
- **出题对战**。直接做会退化成「专挑最阴间的」。解法是让出题方也可能输:对方
  4–5 次猜中 → 满分;1–2 次就中(太简单)或根本没猜中(太冷门)→ 零分。
  出题从整人变成读心。
- **分享战绩**。现有的 Wordle 式彩块用户评价「屁用没有」,判断正确 —— Wordle 能
  传播是因为全网当天同一个词、方块形状本身在讲故事。替代方案是**挑战链接**
  (谜底 id 编码进 URL,朋友点开是同一个谜底,猜完自动比)。
- 照片模糊逐步解锁 / 猜第五人 / 猜年份(给某年 top20 榜单猜年份,数据现成)/
  生存模式 / 闪电战 / Higher-Lower / 反向 20 问(信息增益决策树,不需要 LLM)/
  彩蛋池(jab 已开头)。

---

## 相关脚本

- `scripts/gen_draft_cards.py` —— 选手卡生成器 + 分布体检 + 冠军阵容指挥唯一性检查
- `scripts/sim_draft.py` —— 早期五档方案的桌面推演。**其档位定义已被 v0.2 取代**,
  但「穷举最优阵容看有没有无脑最优解」这个检验方法仍然有用:当时 300 个池子跑出
  10 种价格组合(38/29/13/9.7/5%…),没有单一霸主。
