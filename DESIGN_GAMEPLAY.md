# 玩法拓展:数据核对与决议记录

**正式设计稿是 `FribergCS2_Blind_Draft_System_Design_v0.2.md`,以那份为准。**
这份只放三样它没有的东西:用真实数据核对出来的数字、讨论中定下的决议、
以及暂时搁置的其它玩法。**没有一条已经实现。**

## 核心判断(整轮讨论的出发点)

**老观众和新手要的东西相反。** 猜选手模式里六个线索维度给完,老观众等于直接
看到答案(只是回忆检索,没有推理);新手给完也还是不知道是谁,因为脑子里没有
650 人的库。用户本人自评接近新手水平,直接玩对战吃力 —— 这是设计的第一约束。

Blind Draft 之所以成立,正是因为它**藏掉身份**:玩的是从残缺信息里推断价值,
而不是回忆人名。新手第一局就能玩,老手也不会腻。

---

## 一、数据核对(2026-08-11/12 实测)

### Grade 分布(v0.2 §3.2 规则,自上而下互斥)

```
        RIFLER  AWPER   IGL    合计
G5         19      7     4      30
G4         19      5     7      31
G3         67     18    19     104
G2         30     11    31      72
G1        226     62    94     382
                            (卡池 619,另有 32 人被排除)
```

v0.2 §3.2 留的那个「G4 规模需要重新跑」的问题:**31 人**。

### 教练:33 人,不是 73

用 `roles` 裸扫会数出 73 个教练/分析师/解说,但那没走游戏的归一化。
按 `primary_role` 实际只有 **33 个 Coach**,而且项目里早就有对应机制 ——
后台编辑器的 `played_role`(选手期位置),**已经标了 86 人**。

```
仍是 Coach 的           33 人
  roles 里还留着位置的    1 人(jR = rifle)
  完全查不到位置的       32 人   ← 生成器直接排除,占全库 4.9%
```

这 32 人可以在后台一个个填掉,UI 是现成的。

### 线索的死线索率(各档内「最大同值占比」,越高越死)

```
        冠军数   Major次数   国籍   俱乐部   年龄
G5       50%      20%       20%    43%     10%
G4       55%      19%       13%    32%     13%
G3       67%      31%       13%    33%     15%
G2      100%      36%       12%    58%     13%
G1      100%      51%       10%    58%      7%
```

**冠军数在 G2/G1 是 100% 死的**,只在 $5/$4 活着。俱乐部在 G2/G1 有 58%
是「自由身」,半死。

一个陷阱:**国籍区分度全表最高,但决策价值最低** —— 知道 $1 卡是乌克兰人,
并不告诉你他值不值一块钱。区分度 ≠ 决策价值,别被这一列骗了。

G1 的 Major 次数分布是 `1 次 207 人 / 2 次 108 / 3 次 50 / 4 次 36` ——
一半是「1 次」,但这恰恰有用:看到「$1 · 打过 1 届」就知道别买。

### Major 冠军数是可用的(曾一度误判)

`placement` 只有 36% 有值,但那不是缺失 —— **上游只记录前八名**,空值等于
「9 名开外」,推出「没拿过冠军」是正确结论。25 场 Major 里 24 场都有冠军记录。
唯一缺口是 **IEM Katowice 2019(Astralis 夺冠)**,补 3 行
`data/major_results.json` 即可 —— 那个文件本来就是干这个的人工修正层。

### 经济锚点

按 v0.2 §17 模板 + §18 权重算:

```
花满 15 块 (G5+G4+G3+G2+G1):  overall 合计 312.6
五个 $1  (5 × G1):            overall 合计 216.0,余 $10
=> 1 块 ≈ 9.7 点 overall
```

**Rogue Buff 的定价必须落在 9.7 附近**:明显更高,所有人打便宜流;明显更低,
没人会 Pass 也没人省钱。同理,缺 IGL 的 debuff 要明显超过 29 点
(一个 $3 IGL 的预算价值),否则「不买指挥、拿钱买 buff」就是正解。

---

## 二、讨论中定下的决议

1. **v1 闭环** —— 用户确认 Buff 卡与后续应用方式已想好,只是想先打磨前半段,
   不存在「省钱没有出口」的问题。
2. **教练** —— 走 `played_role` 回退;回退不到的 32 人排除出卡池。
3. **线索分配** —— 放弃 v0.2 §7.2「每轮五种线索各用一次」的硬约束。那条和
   §7.4「不要死线索」直接打架:每轮必有 $1/$2 卡,而这两档冠军数 100% 为 0。
   改成**按档位加权**:$5/$4 可用冠军数,$3 少用,$2/$1 禁用。
4. **保护名单** —— s1mple / donk / ZywOo 这类锁死 $5,不参与降档。
5. **IGL 限价** —— 从 $3 提到 $4。原因:同档下 IGL 与步枪强度几乎相等
   (G5:80.3 vs 82.2),只压价格不压强度会造出系统性错价,
   「$3 IGL 优先看」会变成玩十把就能悟到的固定最优解。提到 $4 后白送幅度
   从约 1.8 块降到 0.8 块,落进噪声范围。
6. **价格差 5 倍 / 强度差 1.9 倍** —— 交给肉鸽方差处理。注意:**方差不改变
   期望**。这条在淘汰制结算下成立(弱队本就该赌方差),在排行榜式线性结算下
   不成立。取决于 v1 的结算形态。
7. **升档** —— 允许。顺带解决卡池深度:G5 只有 30 人、每局消耗 6 张,约 5 局
   就见完一轮;允许 G4 升上来后 $5 那一格的来源变成 61 人,新鲜度翻倍。
8. **阵容是软惩罚** —— 可以不买 IGL/AWP,但 Reveal 阶段吃 debuff。因此 Board
   生成器**不需要位置兜底**,比硬约束简单得多。

### 单独的卡片数据库:要,但必须是生成物

这个项目已经有两个权威源(爬虫生成物 / 人工层),再加一个手工维护的第三方
数据库一定会烂掉。照抄现有模式:

```
players.json + hltv_top20.json + 人工层
        ↓  scripts/gen_draft_cards.py(可重跑)
data/draft_cards.json             ← 生成物,提交进仓库
data/manual/draft_overrides.json  ← 人工平衡层,挂可写卷,后台可改不用重新部署
```

**幂等是硬要求**。v0.2 §16.1 说卡牌 RNG 只跑一次、之后永久固定;如果生成脚本
每次重跑都换随机种子,650 张卡会在一次部署后集体悄悄变样,玩家认知全部作废,
而且不会有任何报错。生成器的种子取自 `hash(page + CARD_VERSION)`,同一个人在
同一版本下永远算出同一张卡;要整体重算就自觉 bump 版本号,那是一次可 diff 的
自觉行为。

---

## 三、生成器现状(`scripts/gen_draft_cards.py`)

已实现 v0.2 §27 的 Step 1/2/3/7:Grade 判定、三职位归一、Grade × Position
模板、§20 的第一版修正、全库分布体检。默认只体检不写盘,`--write` 才产出
`data/draft_cards.json`。

**已修的坑**:第一版修正项按名次线性加分,导致 G5 火力中位 97、前 15 名清一色
99,顶死天花板 —— 正好违反 §27 Step 7 的「G5 不会人人接近 99」。原因是 G5 基础
火力 89、离上限只剩 10 点,却给了最多 17 点加成。改成先归一化再乘一个固定的
档内幅度(`FIRE_SPREAD = 6.0`),档间距离由模板负责,修正项只管档内排序。

修完的体检:

```
        火力  领导  经验  稳定    overall 最低~中位~最高
G5       92    29    85    85      82.8 ~ 86.2 ~ 89.9
G4       80    28    71    74      72.0 ~ 75.4 ~ 80.1
G3       71    24    54    64      60.7 ~ 64.5 ~ 69.1
G2       58    24    42    58      50.9 ~ 54.7 ~ 57.2
G1       52    21    24    48      41.1 ~ 44.5 ~ 48.4

RIFLER 中位 45.1 | AWPER 47.1 | IGL 46.1     ← 没有位置统治
```

**还需要人判断的**:overall 前 15 里 **Magisk 排第 2(G5 IGL,领导 97)** ——
他历史上是步枪手不是指挥,`roles` 里的 igl 标签把他推上了 IGL 模板。这正是
§21 说的「算法明显违背玩家常识」,是第一个 Override 候选。抽查名单里还得
过一遍传奇 IGL、Top20 转 IGL、老传奇这几类。

---

## 已撤回

**线索采购模式**(开局什么都不给,花预算逐条买线索)。死结:猜错之后给不给
属性对比?给 → 猜一次白拿一整行,买卖失去意义;不给 → 新手在 650 人里盲猜,
比现在更绝望。矛盾在结构上,调价格调预算都救不了。

**早期的「档位 = 池子 + 价格 + 线索」三合一设计**。已被 v0.2 的
Grade / Price / Power 三分离取代 —— 后者明显更对。

---

## 暂存(未展开)

- **跨模式的统计 / 图鉴层**。用户明确要,且要求「不同模式里都可以有」——
  这句话定了架构:必须先做成一层**共享存档**,否则做到第三个模式就得推倒重来。
- **出题对战**。直接做会退化成「专挑最阴间的」。解法是让出题方也可能输:
  对方 4–5 次猜中 → 满分;1–2 次就中(太简单)或根本没猜中(太冷门)→ 零分。
  出题从整人变成读心。
- **分享战绩**。现有的 Wordle 式彩块用户评价「屁用没有」,判断正确 ——
  Wordle 能传播是因为全网当天同一个词、方块形状本身在讲故事。替代方案是
  **挑战链接**(谜底 id 编码进 URL,朋友点开是同一个谜底,猜完自动比)。
- 照片模糊逐步解锁 / 猜第五人 / 猜年份(给某年 top20 榜单猜年份,数据现成)/
  生存模式 / 闪电战 / Higher-Lower / 反向 20 问(信息增益决策树,不需要 LLM)/
  彩蛋池(jab 已开头)。

## 相关脚本

- `scripts/gen_draft_cards.py` —— 选手卡生成器 + 分布体检
- `scripts/sim_draft.py` —— 早期五档方案的桌面推演。**其档位定义已被 v0.2
  取代**,但「穷举最优阵容看有没有无脑最优解」这个检验方法仍然有用:
  当时 300 个池子跑出 10 种价格组合(38/29/13/9.7/5%…),没有单一霸主。
