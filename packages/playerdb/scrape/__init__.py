# -*- coding: utf-8 -*-
"""建库与抓图的爬虫。**不进运行时镜像**。

和 playerdb 其余部分的区别是「谁需要它」：
  playerdb.players / regions / rankings   服务端每个请求都在用 -> 必须进镜像
  playerdb.scrape.*                       只在本机维护数据时跑 -> 不进镜像

分成子包是为了让这条边界能被 .dockerignore 用一行表达出来。混在一起的后果不是
镜像大一点——是容器里明明只读、又缺 Pillow,爬虫却看起来「可以跑」,管理页那个
「数据更新」按钮会在生产上以一种很难看懂的方式失败。
"""
